# Hosty Status (Vercel Test)

Simple deployment test app for GitHub + Vercel.

## What it does
- Serves a status dashboard at `/`
- Serves JSON status at `/api/status`

## Deploy
Connected to Vercel via CLI.
